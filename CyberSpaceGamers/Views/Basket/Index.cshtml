@model IEnumerable<CyberSpaceGamers.Models.BasketItem>    // connects this page to a list of BasketItem objects so we can show all items in the basket

@{
    ViewData["Title"] = "Your Basket";     // sets page title
    Layout = "_Layout";      // uses the main layout page
}
  
<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>Your Basket</h2>
        <a asp-controller="Product" asp-action="Index" class="btn btn-outline-primary">← Back to Products</a>   <!-- link to go back to the product list -->
    </div>

   
    <!--Checkout Message Display-->
    @if (TempData["CheckoutMessage"] != null)
    {
        <div class="alert alert-success mt-2">
            @TempData["CheckoutMessage"]
            <a id="viewOrdersBtn" asp-controller="Basket" asp-action="Orders">View Your Order</a>
        </div>
        
    }
    else if (Model == null || !Model.Any())  // if basket is empty -->
    {
        <div class="alert alert-info">Your basket is empty.</div>
    }

            // if basket has items -->
    else
    {
        <div class="container py-4">

            @foreach (var item in Model)
            {
                <!-- Basket list -->
                <div class="list-group mb-4">
                    <div class="list-group-item d-flex justify-content-between align-items-center">
                    <div>
                        <div class="fw-semibold">@item.Product.Name</div>
                        <div class="text-muted small">@item.Product.Price</div>
                    </div>
                    <!-- X on the right -->
                        <form asp-action="Remove" asp-route-id="@item.Id" method="post">
                            <button class="btn btn-outline-danger btn-sm w-100"><i class="bi bi-trash"></i></button>
                        </form>
                    
                </div>
            </div>
            }
            <!-- Bottom area with Checkout button -->
            <div class="card mt-4 shadow-sm">
                <div class="card-body text-end">
                    <h4 class="text-dark">Total: @Model.Sum(i => i.Product.Price * i.Quantity).ToString("C")</h4>                    
                </div>                
            </div>
        <!-- checkout button -->
            <form asp-action="Checkout" method="post" class="text-end mt-3 mb-5">
                <button class="btn btn-success btn-lg">Checkout</button>
            </form>            
        </div>
    }
</div>

<!--Hides the view my order link after 5 seconds-->
<script> 
    // Wait 1 second before showing the button
    setTimeout(() => {
        const btn = document.getElementById('viewOrdersBtn');
        btn.style.display = 'inline-block';

        // Hide the button after 5 seconds
        setTimeout(() => {
            btn.style.display = 'none';
        }, 5000);
    }, 1000);
</script>
